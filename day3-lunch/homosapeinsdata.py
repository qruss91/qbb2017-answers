#!/usr/bin/env python

import sys

total = 0
count = 0

for l in sys.stdin:
    if sys.argv[-1] == "-a"
    l = l.rstrip()
    total += mylen
    count += 1
if sys.argv[-1] == "-a":
    print total/count 